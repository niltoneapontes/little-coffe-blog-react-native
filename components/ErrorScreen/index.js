import React from 'react';
import { ErrorContainer, ErrorText } from '../ErrorScreen/styles';

const ErrorScreen = () => {
  return (
    <ErrorContainer>
      <ErrorText>Vishe... Não consegui carregar o blog. 😥</ErrorText>
    </ErrorContainer>
  )
}

export default ErrorScreen
